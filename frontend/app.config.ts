export default defineAppConfig({
  ui: {
    primary: 'blue',
    gray: 'neutral',
    colors: ['critical', 'fatal', 'error', 'warning', 'info', 'debug', 'success'],
    notifications: {
      position: 'top-0 right-0'
    }
  }
})
